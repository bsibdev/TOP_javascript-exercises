const sumAll = function(inta,intb) {
// if else statement to assign start and end to correct ints 
if (inta <= intb) {
    let start = inta;
    let end = intb;
    } else if (inta > intb) {
        let start = intb;
        let end = inta;
    }else {return "ERROR"

    }

}
//
};

// Do not edit below this line
module.exports = sumAll;
